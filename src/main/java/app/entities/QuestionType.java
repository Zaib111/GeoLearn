package app.entities;

/**
 * Represents the format of a quiz question.
 * MCQ = user selects from multiple choices.
 * TYPE_IN = user types an answer manually.
 */
public enum QuestionType {
    MCQ,
    TYPE_IN
}
