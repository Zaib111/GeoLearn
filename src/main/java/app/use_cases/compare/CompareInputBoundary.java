package app.use_cases.compare;

public interface CompareInputBoundary {

    /**
     * Execute the Compare use case with the given input data.
     */
    void execute(CompareInputData inputData);
}
